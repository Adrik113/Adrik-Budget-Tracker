import React from 'react';

function ClearIncomeButton({onClick}) {
    return (
        <button onClick={onClick}>
            Clear Income
        </button>
    );
}

export default ClearIncomeButton; 