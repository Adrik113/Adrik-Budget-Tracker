import React from 'react';

const IncomeInputField = ({value, onChange}) => (<input type="text" placeholder="Enter monthly income" value={value} onChange={onChange} />);

export default IncomeInputField;
